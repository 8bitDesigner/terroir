{
  "name": "terroir",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "homepage": "http://terrior.s3-us-west-2.amazonaws.com",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Paul Sweeney <paul@8-bitdesign.com>",
  "license": "ISC",
  "dependencies": {
    "chalk": "^2.0.1",
    "react": "^15.6.1",
    "react-dom": "^15.6.1"
  },
  "devDependencies": {
    "react-scripts": "1.0.10"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "npm run build && aws s3 sync ./build/ s3://terrior --profile admin",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
